services:
  redis:
    image: redis:3.2.12
    restart: always
    container_name: redis
    command: redis-server /usr/local/etc/redis/redis.conf
    volumes:
      - ./data:/data
      - ./conf:/usr/local/etc/redis
      - ./tmp:/tmp
    ports:
      - "6379:6379"